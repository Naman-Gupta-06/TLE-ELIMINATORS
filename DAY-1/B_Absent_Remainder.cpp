// CODE BY @namangupta28
#include <bits/stdc++.h>
using namespace std;

void solve()
{
   int n;
   cin >> n;
   int arr[n];
   for (int = 0; i < n; i++)
   {
      cin >> arr[i];
   }
   vector<pair<int, int>> v;
   sort(a, a + n,greater<int>);
   for (int i = 0; i < n;i++){
      int x = a[i];
   }
}
int main()
{
   int t;
   cin >> t;
   while (t--)
      solve();
   return 0;
}